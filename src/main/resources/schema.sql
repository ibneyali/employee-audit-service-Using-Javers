-- Create ADDRESS table
CREATE TABLE IF NOT EXISTS ADDRESS (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    ADDRESS_LINE_1 VARCHAR(255) NOT NULL,
    ADDRESS_LINE_2 VARCHAR(255),
    ADDRESS_LINE_3 VARCHAR(255),
    COUNTRY VARCHAR(100) NOT NULL,
    POSTAL_CODE VARCHAR(20),
    CREATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_BY VARCHAR(100)
);

-- Create DEPARTMENT table
CREATE TABLE IF NOT EXISTS DEPARTMENT (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR(255) NOT NULL,
    CREATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_BY VARCHAR(100)
);

-- Create TRAINING table
CREATE TABLE IF NOT EXISTS TRAINING (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NAME VARCHAR(255) NOT NULL,
    DESCRIPTION VARCHAR(1000),
    CREATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_BY VARCHAR(100)
);

-- Create EMPLOYEE table
CREATE TABLE IF NOT EXISTS EMPLOYEE (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FIRST_NAME VARCHAR(100) NOT NULL,
    LAST_NAME VARCHAR(100) NOT NULL,
    EMAIL VARCHAR(255) UNIQUE NOT NULL,
    PHONE VARCHAR(20),
    DEPARTMENT_ID BIGINT,
    ADDRESS_ID BIGINT,
    CREATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_BY VARCHAR(100),
    VERSION INT DEFAULT 0,
    FOREIGN KEY (DEPARTMENT_ID) REFERENCES DEPARTMENT(ID),
    FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS(ID)
);

-- Create EMPLOYEE_TRAINING table
CREATE TABLE IF NOT EXISTS EMPLOYEE_TRAINING (
    ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    EMP_ID BIGINT NOT NULL,
    TRAINING_ID BIGINT NOT NULL,
    DATE_OF_ALLOCATION DATE,
    STATUS VARCHAR(20),
    CREATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_TIMESTAMP TIMESTAMP NOT NULL,
    UPDATED_BY VARCHAR(100),
    FOREIGN KEY (EMP_ID) REFERENCES EMPLOYEE(ID),
    FOREIGN KEY (TRAINING_ID) REFERENCES TRAINING(ID)
);
-- Create AUDIT_TABLE for auditing purposes
CREATE TABLE IF NOT EXISTS AUDIT_TABLE (
    EVENT_ID BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    EVENT_DOMAIN VARCHAR(100),
    EVENT_ENTITY VARCHAR(100),
    EVENT_ENTITY_ID BIGINT,
    EVENT_NAME VARCHAR(50),
    EVENT_PAYLOAD CLOB,
    EVENT_SUMMARY VARCHAR(255),
    EVENT_ENTITY_VERSION INT,
    EVENT_TIMESTAMP TIMESTAMP,
    ENTRY_TIMESTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    EVENT_INITIATOR VARCHAR(100)
);


-- Create indexes for better performance
CREATE INDEX IF NOT EXISTS IDX_EMPLOYEE_EMAIL ON EMPLOYEE(EMAIL);
CREATE INDEX IF NOT EXISTS IDX_EMPLOYEE_DEPARTMENT ON EMPLOYEE(DEPARTMENT_ID);
CREATE INDEX IF NOT EXISTS IDX_EMPLOYEE_TRAINING_EMP ON EMPLOYEE_TRAINING(EMP_ID);
CREATE INDEX IF NOT EXISTS IDX_EMPLOYEE_TRAINING_TRAINING ON EMPLOYEE_TRAINING(TRAINING_ID);

-- JaVers sequences (required for JaVers to work properly)
CREATE SEQUENCE IF NOT EXISTS jv_global_id_pk_seq AS BIGINT START WITH 1;
CREATE SEQUENCE IF NOT EXISTS jv_commit_pk_seq AS BIGINT START WITH 1;
CREATE SEQUENCE IF NOT EXISTS jv_snapshot_pk_seq AS BIGINT START WITH 1;

-- JaVers tables for audit tracking
CREATE TABLE IF NOT EXISTS jv_global_id (
    global_id_pk BIGINT NOT NULL PRIMARY KEY,
    local_id VARCHAR(191),
    fragment VARCHAR(200),
    type_name VARCHAR(200),
    owner_id_fk BIGINT,
    FOREIGN KEY (owner_id_fk) REFERENCES jv_global_id(global_id_pk)
);

CREATE INDEX IF NOT EXISTS jv_global_id_local_id_idx ON jv_global_id(local_id);
CREATE INDEX IF NOT EXISTS jv_global_id_type_name_idx ON jv_global_id(type_name);

CREATE TABLE IF NOT EXISTS jv_commit (
    commit_pk BIGINT NOT NULL PRIMARY KEY,
    author VARCHAR(200),
    commit_date TIMESTAMP,
    commit_date_instant VARCHAR(30),
    commit_id NUMERIC(22,2)
);

CREATE INDEX IF NOT EXISTS jv_commit_commit_id_idx ON jv_commit(commit_id);

CREATE TABLE IF NOT EXISTS jv_commit_property (
    property_name VARCHAR(191) NOT NULL,
    property_value VARCHAR(600),
    commit_fk BIGINT NOT NULL,
    PRIMARY KEY (commit_fk, property_name),
    FOREIGN KEY (commit_fk) REFERENCES jv_commit(commit_pk)
);

CREATE INDEX IF NOT EXISTS jv_commit_property_commit_fk_idx ON jv_commit_property(commit_fk);
CREATE INDEX IF NOT EXISTS jv_commit_property_property_name_idx ON jv_commit_property(property_name);

CREATE TABLE IF NOT EXISTS jv_snapshot (
    snapshot_pk BIGINT NOT NULL PRIMARY KEY,
    type VARCHAR(200),
    version BIGINT,
    state CLOB,
    changed_properties CLOB,
    managed_type VARCHAR(200),
    global_id_fk BIGINT NOT NULL,
    commit_fk BIGINT NOT NULL,
    FOREIGN KEY (global_id_fk) REFERENCES jv_global_id(global_id_pk),
    FOREIGN KEY (commit_fk) REFERENCES jv_commit(commit_pk)
);

CREATE INDEX IF NOT EXISTS jv_snapshot_global_id_fk_idx ON jv_snapshot(global_id_fk);
CREATE INDEX IF NOT EXISTS jv_snapshot_commit_fk_idx ON jv_snapshot(commit_fk);

